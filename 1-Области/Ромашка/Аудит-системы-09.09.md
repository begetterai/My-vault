# Аудит работающей системы — 09.09.2026

Полная проверка Mini App: сервер, механика смены, клиент и бот — плюс
чтение боевых данных за 04–09.09. Факты и код сошлись: то, что в данных
выглядит как «работает наполовину», объясняется конкретными строками.

## Что показали данные

| Факт | Причина |
|---|---|
| День закрылся **1 раз из 6** | Лист закрытия управляющего блокировал +5 всей точке. У него нет срока → нет напоминания → забывается |
| Ложные «просрочено» каждую ночь | `tick` считал по календарной дате, данные пишутся по операционной. С 00:00 до 05:00 сданные листы «исчезали» |
| **ОВИР: 6 человек, 0 отметок** за 6 дней | Срок закрытия 03:30 — окно напоминаний целиком в сломанном интервале, шквал ложных тревог каждую ночь |
| **Копий нет 18 дней** (с 22.08) | Отметка «сделано» ставилась до копирования: одна ошибка съедала сутки |
| 68 из 171 начисления — `too_fast` (40 %) | Не дефект: прокликивание чек-листов. Сигнал никуда не идёт, балл нулевой |
| Задачи из находок — **0 строк** | Закрытая задача воскресала каждый вечер, пока провалы не выпадут из окна |
| Обучение стоит с 29.08 | Организационное |
| Заполняются 6 листов из 105 | Организационное |

## Исправлено 09.09

| Что | Где |
|---|---|
| Копии: отметка только после успеха, повтор раз в 10 минут, отдельная тревога | `schedule.py` |
| Ночные ложные тревоги: операционный день вместо календарного | `schedule.py` |
| Управляющий получает напоминания по своим листам; санитарный снова влияет на закрытие дня | `storage.py` |
| Лист управляющего не блокирует +5 всей смене | `score.py` |
| Письмо «Смена не закрыта» снова доходит — в строке была станция вместо точки | `schedule.py` |
| Запись в таблицу проверяет ответ Google и повторяет: уход, часы, подтверждения и пароли больше не теряются молча | `storage.py` |
| Без геолокации показывается кнопка «попросить управляющего» — человек не заперт | `webapp.py`, `index.html` |
| Цеховому закрытию дана отсрочка 15 минут вместо мгновенной просрочки | `build_shift_forms.py` |

Копия боевой таблицы за 09.09 создана вручную — дыра закрыта.

## Исправлено 09.09, вторая волна

**«У Владимира пропало закрытие» — причина найдена и она поучительная.**
Приложение делит формы на два списка: ежедневные и редкие, и признаком
ежедневного был **только срок**. 07.09 по решению Азиза у листа
«Управляющий · Закрытие» сняли время — и лист уехал в редкие формы,
где его никто не ищет. Лист не пропадал, он оказался не там, где его
ждут. Теперь признак ежедневного — срок **или** стадия, а `selfcheck`
ругается, если лист со стадией не попадает в основной список.

| Что | Где |
|---|---|
| Закрытие управляющего вернулось в основной список | `index.html` |
| Проверка ловит лист, который не доходит до человека | `selfcheck.py` |
| Обрыв связи не убивает кнопку: 20 запросов идут через `jpost`, сбой превращается в понятный ответ | `index.html` |
| Состав смены: флаг снимается и при ошибке — экран больше не «умирает» | `index.html` |
| Фото в журнале и бланке — только камера, файловых полей не осталось ни одного | `index.html` |
| Текст «сделанное не потеряется» убран оттуда, где черновика нет | `index.html` |
| Один знак «меньше» в комментарии больше не отменяет уведомление | `bot.py` |
| Длинные ответы («Задачи», «На проверке», KPI, «История») режутся и доходят | `bot.py` |
| Фото из бота не сохранилось — просим переснять вместо пометки «есть» | `bot.py` |
| На непонятое сообщение бот отвечает, а не молчит | `bot.py` |
| Состав смены: сбой чтения больше не стирает лист, добавлен замок | `roster.py` |
| Работа на чужой точке — не «не вышел» | `roster.py` |
| Проверки при подтверждении листа не открываются при сбое чтения | `webapp.py` |
| Строгое чтение там, где человека запирают: уход, приём смены | `storage.py`, `webapp.py` |
| Замок на операции «прочитал → изменил → записал» (5 функций) | `storage.py` |
| +5 за день — только тем, кто сдавал листы | `score.py` |
| Напоминания по занятой станции; передача/приём не гоняются у тех, кто работает один | `schedule.py` |
| Ночное обслуживание догоняется, если перезапуск попал в его окно | `schedule.py` |

Сборка на сервере — `v09.09-3`, проверка чистая.

## Осталось в очереди

- `close_day` считает листы точки, а не человека: +5 получает и тот, кто
  не сдал ничего.
- Напоминания адресуются по отделу, а не по занятой станции; `give`/`take`
  гоняются даже когда человек работает один.
- Окно ночного обслуживания 30 минут: рестарт в это время — сутки без
  закрытия, отрезки остаются открытыми.
- Клиент: обрыв сети оставляет кнопку мёртвой и теряет бланк или журнал —
  черновиков у них нет (15 кнопок).
- `ROSTER.dirty` при ошибке навсегда останавливает обновление экрана.
- Текст «сделанное не потеряется» показывается там, где черновика нет.
- Галерея всё ещё доступна в журнале и бланке — правку 06.09 применили
  только к чек-листам.
- Бот: незаэкранированный HTML (один знак «меньше» в комментарии отменяет
  уведомление второму контуру); длинные ответы «На проверке» и KPI не
  доходят; фото теряется с пометкой «есть».

## Что переносить в сайт, а не чинить здесь

Состояние бота в памяти, глобальный замок на весь процесс, расход квоты
Google, идентификация человека по имени, отсутствие черновиков. Это
архитектура — её переделывает [[Сайт-дорожная-карта]].

## Что сделано хорошо (не трогать)

Проверка подписи Telegram, сессии, PBKDF2 — по учебнику. Кэш с ключом по
версии листа. Сужение замка при сдаче листа. Серверная проверка цепочки
открытие → передача → приём → закрытие, включая «работает один». Расчёт
часов через полночь. Посекционный рендер вместо белого экрана. Черновик
чек-листа с честным диалогом восстановления. Дублей объявлений в JS нет.
